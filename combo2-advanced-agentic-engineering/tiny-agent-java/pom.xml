<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>ai.octoco.tinyagent</groupId>
  <artifactId>tiny-agent-java</artifactId>
  <version>1.0.0-SNAPSHOT</version>
  <packaging>pom</packaging>
  <name>Tiny Agent (Java)</name>
  <description>Module 8 tiny coding agent — Java path</description>

  <modules>
    <module>shared</module>
    <module>starter</module>
    <module>reference</module>
    <module>tests</module>
  </modules>

  <properties>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <maven.compiler.release>21</maven.compiler.release>
    <junit.version>5.11.4</junit.version>
    <jackson.version>2.18.9</jackson.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <dependency>
        <groupId>ai.octoco.tinyagent</groupId>
        <artifactId>tiny-agent-shared</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>ai.octoco.tinyagent</groupId>
        <artifactId>tiny-agent-starter</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>ai.octoco.tinyagent</groupId>
        <artifactId>tiny-agent-reference</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.fasterxml.jackson.core</groupId>
        <artifactId>jackson-databind</artifactId>
        <version>${jackson.version}</version>
      </dependency>
      <dependency>
        <groupId>org.junit.jupiter</groupId>
        <artifactId>junit-jupiter</artifactId>
        <version>${junit.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>3.13.0</version>
          <configuration>
            <release>${maven.compiler.release}</release>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>3.5.2</version>
          <configuration>
            <!-- Eval tests (if any) are tagged and excluded from the default suite. -->
            <excludedGroups>evals</excludedGroups>
          </configuration>
        </plugin>
        <plugin>
          <groupId>org.codehaus.mojo</groupId>
          <artifactId>exec-maven-plugin</artifactId>
          <version>3.5.0</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-wrapper-plugin</artifactId>
          <version>3.3.2</version>
        </plugin>
      </plugins>
    </pluginManagement>
    <plugins>
      <!-- `exec:java` invoked directly from the CLI runs against every
           reactor project. This aggregator pom, `shared` and `tests` have no
           main class, so declare the plugin here with skip=true; child
           plugin declarations inherit this and starter/reference override
           it back to false alongside their own mainClass. `mainClass` is a
           required parameter even when skipped, so a placeholder is supplied. -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>exec-maven-plugin</artifactId>
        <configuration>
          <skip>true</skip>
          <mainClass>none</mainClass>
        </configuration>
      </plugin>
    </plugins>
  </build>
</project>
